I created this repo to verify the behavior of setting up a mon-repo and to see if we can define a mono repo with tools and api in this single repo. 

- add to the mono repo build tools, reproduce https://github.com/cdoan1/hyperfleet-api-codegen into this mono repo structure. 
- define an api that the build tools generator can can act on.
- define an sdk for the api
- define a cli that leverages the sdk and generated types from the passthrough generators.
- add comprensive unit tests for all components
- add comprensive e2e tests to ensure the mono-repo delivers on the requirements.
- the api has seperate module dependency form the other tools in this repo.
- report on the e2e results, progress in e2e completion shows progress in the mono-repo project.
- this is a golang project

style rules:

- create ./docs directory to capture design and design decisions.
- update the README.md as appropriate
- Keep this generation prompt at the bottom of the README.md file so it can be edited and re-run as needed.
